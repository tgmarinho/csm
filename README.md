# CSM Psicologia — site estático

Conteúdo em `public/` (HTML, CSS, JS e assets).

## Scripts

- `npm start` — servidor local em [http://localhost:3000](http://localhost:3000)
- `npm run deploy` — publica `public/` na branch `gh-pages` (GitHub Pages)
